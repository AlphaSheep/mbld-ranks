branches:
  main:
    regex: ^main$
    mode: ContinuousDeployment
    label: ''
    increment: Patch
    is-main-branch: true
  feature:
    mode: ManualDeployment
    label: '{BranchName}'
    increment: Inherit
    is-main-branch: false
ignore:
  sha: []
